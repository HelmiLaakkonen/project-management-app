import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      ChangePasswordButton: {
        main: '#cce4fe', //Light blue
      },
      DeleteButton: {
        main: '#ffb6c1', // Light red
      },
    },
});

export default theme;

